import { sizes } from './sizescheme'

export { colors } from './default-colorscheme'
export { sizes } from './sizescheme'

export const { 9: columnWidth } = sizes
export const { 9: rowHeight } = sizes
